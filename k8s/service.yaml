apiVersion: v1
kind: Service
metadata:
  name: dialogos
  namespace: dialogos
  labels:
    app: dialogos
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: dialogos
