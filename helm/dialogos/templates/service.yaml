apiVersion: v1
kind: Service
metadata:
  name: {{ include "dialogos.fullname" . }}
  labels:
    {{- include "dialogos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
  selector:
    {{- include "dialogos.selectorLabels" . | nindent 4 }}
