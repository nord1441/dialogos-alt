apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dialogos.fullname" . }}
  labels:
    {{- include "dialogos.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
